services:
  doochybot:
    build: .
    image: doochybot:latest
    container_name: doochybot
    restart: unless-stopped
    env_file:
      - .env.docker
    volumes:
      - ./data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
